# FRPN Pipelines

- `bcdb/`: BCDB classification and homopolymer regression workflows.
- `md_final1640_v2/`: final MD benchmark workflows.

The pipeline directories preserve the original local-import behavior by being placed on `sys.path` by the CLI runner. This keeps behavior equivalent while making the repository structure publication-ready.
